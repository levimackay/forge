/// Identifies the module. A placeholder so the package has a public surface
/// to test against before the domain model lands in issue #2.
public enum ModuleInfo {
    public static let name = "ForgeKit"
}
